void main() {
	int N, score;
	scanf("%d", &N);
	if (N >= 0 && N <= 10) {
		score = N * 6;
	}
	else if (N >= 11 && N <= 20) {
		score = (N - 10) * 2 + 60;
	}
	else if (N >= 21 && N <= 40) {
		score = (N - 20) * 1 + 80;
	}
	else {
		score = 100;
	}
	printf("%d", score);
}

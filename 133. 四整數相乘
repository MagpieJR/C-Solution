mymul.h:

int mymul(int* a, int size);

mymul.c:

#include "mymul.h"
int mymul(int* a, int size) {
    int result = 1;
    for (int i = 0; i < size; i++) {
        result *= a[i];
    }
    return result;
}

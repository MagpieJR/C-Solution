D_size.h:

int D_size(int S, int D);

D_size.c:

#include "D_size.h"
int D_size(int S, int D) {
    int start = 1;
    while (1) {
        if (D < (start + S)) {
            return S;
            break;
        }
        else {
            start += S;
            S += 1;
        }
    }
}
